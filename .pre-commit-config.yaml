repos:
  - repo: https://github.com/doublify/pre-commit-rust
    rev: v1.0
    hooks:
      - id: fmt
        args: ["--manifest-path=Cargo.toml", "--"]
      - id: clippy
        args: ["--manifest-path=Cargo.toml", "--all-targets", "--", "-W", "clippy::all"]
